[pytest]
testpaths = tests
# Slow tests need model WEIGHTS (a multi-GB download). Deselected by default so
# `pytest` is always safe to run offline; opt in with `pytest -m slow`.
addopts = -m "not slow"
markers =
    slow: requires model weights; downloads on first run
